name: moneytrack
description: Fund performance tracker — signals state management, dummy data (Supabase in iteration 2).
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  # Reactive state management: signal / computed / Watch.
  signals: ^6.0.2
  # Date formatting for the monthly history table.
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
